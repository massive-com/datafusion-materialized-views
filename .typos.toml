# typos configuration file
# Place this file in the project root (same level as Cargo.toml)
[default]

# Ignore entire Cargo.toml to avoid false positives on git revision hashes
[default.extend-exclude]
paths = ["Cargo.toml"]
